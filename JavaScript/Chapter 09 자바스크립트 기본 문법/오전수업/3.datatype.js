/**
 * 데이터 타입의 종류
 * number 숫자
 * string 문자
 * boolean 참, 거짓
 * null
 * undefined 정의되지 않았다
 * symbol
 * object 객체
 */

let num = 1;
let name = 'saeun';
let isFree = false;
let sample;
console.log('sample-', sample); // undefined

const obj1 = {
  a: 'test',
};

console.log(obj1.a);
